<div class="page-content">
    <div class="panel panel-boxed">
        <div class="panel-body">
            <form method="post" action="/admin/saveStripeSetting">
                <div class="form-group">
                    <label>Public Key</label>
                    <input type="text" name="public_key" value="<%=stripe_public_key%>" class="form-control">
                </div>
                <div class="form-group">
                    <label>Secret Key</label>
                    <input type="text" name="secret_key" value="<%=stripe_secret_key%>" class="form-control">
                </div>
                <div class="form-group">
                    <label class="form-control-label">Stripe Webhook ID</label>
                    <input type="text" class="form-control" name="stripe_webhook_id" value="<%= stripe_webhook_id %>" placeholder="whsec_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX" required>
                    <small class="form-text text-muted">
                        Create a webhook in your Stripe dashboard and paste the webhook signing secret here.
                        <br>Webhook URL should be: <code>https://your-domain.com/stripe-webhook</code>
                    </small>
                </div>
                <div class="form-group">
                    <button class="btn btn-primary">Save</button>
                </div>
            </form>
        </div>
    </div>
</div>
